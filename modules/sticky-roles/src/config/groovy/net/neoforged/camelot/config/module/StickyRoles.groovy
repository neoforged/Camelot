
package net.neoforged.camelot.config.module

import groovy.transform.CompileStatic

/**
 * The sticky roles module.
 * This module allows configuring roles that will be kept (and therefore "stick") when a user joins the server after leaving it.
 */
@CompileStatic
class StickyRoles extends ModuleConfiguration {
}
