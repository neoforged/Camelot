package net.neoforged.camelot.config.module

import groovy.transform.CompileStatic

/**
 * The module for thread pings.
 * <p>
 * Thread pings allow configuring users or roles that should be mentioned automatically when threads
 * are created in a channel.
 */
@CompileStatic
class ThreadPings extends ModuleConfiguration {
}
